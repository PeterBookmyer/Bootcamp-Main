//custome moduel - code we have built ourselves
//remote module - code from other devs we can use in apps
//shopped modules - there come with the node installatio - fs module

const badmath = require('badmath');

console.log(badmath.pie);

console.log(badmath.predictable());
